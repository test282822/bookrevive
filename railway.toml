[build]
builder = "nixpacks"
buildCommand = "python3.11 -m pip install -r requirements.txt"

[deploy]
startCommand = "python3.11 -m uvicorn api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[source]
rootDirectory = "backend"
