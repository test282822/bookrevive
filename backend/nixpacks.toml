[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "tesseract", "calibre"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python3.11 -m uvicorn api:app --host 0.0.0.0 --port $PORT"
